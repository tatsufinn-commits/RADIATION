# 02-analyze/ — ANALYZE jurisdiction (decomposer)
**Expected artifacts:** analysis matrices, claim tables, comparison tables,
dependency maps. Every row traces to a Research finding ID (S-n / C-n from
proc_research-sortie sheets). Filename: `YYYY-MM-DD_<topic-slug>_matrix.md`.
Active in: @Data (lean), @Gather, @Decode, @Radiation.
