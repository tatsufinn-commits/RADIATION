# NEURON ARCHIVE (`scaffolding/neurons/_archive/`)
Completed TID relay cycles, faded per AI_RULES II.10.4. Records are never deleted —
the active set in `sensoryneurons/ · interneurons/ · motorneurons/` holds the current
cycle only; check 27 judges the active set. Faded 4300 (2026-09-14).

RETRIEVAL RULE (5300): the active neuron regions (`sensoryneurons/ · interneurons/ · motorneurons/`) are the ordinary working context; `_archive/` replicas are evidence/history and must never be loaded into an assistant context alongside their canonical twin (REPLICA_MANIFEST.json — 12 hash-bound pairs).
