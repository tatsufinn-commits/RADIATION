# WORKFLOW — Per-Area Detail (WP-1 1.2)
**Basis:** AGENTS.md layered cascade per 1.6 checklist scaffolding/checklists/WP1_1.6_CHANGE_CONTROL_CHECKLIST.md
**Law:** Sequence as-written WP-1→WP-2.3→WP-4, warn-and-justify, quarantine-before-delete

## Steps

1. **Boot** per docs/.readme Tier 0 per III.1 — read .readme before anything else, Boot Sequence follows, work before boot invalid per III.1
2. **Route** per agents/AGENT_INDEX.md — exact routing decision, invariants tested check 38, unknown-host fallback safe and neutral
3. **Pass** — RADIATION PASS → python3 agents/_common/radiation_pass.py --host "<label>" — state machine not elevation, zero writes, yields observed context/tools, task/effect boundary, profile, verification commands, non-availability results
4. **Scan** — Five-phase Autonomous Scan docs/CUE_SYSTEM.md: COMPREHEND → EXTRACT (4-step protocol) → ASSESS → CONFIDENCE GATE → SCAN DECLARATION per III.3, Edge-Case Rulebook E1–E8 annexed, on LOW confidence ask never guess
5. **Control plane** — Route work through II.11 control plane docs/CONTROL_PLANE.md — two keys per effect policy+tool, chained decision + content-bound single-use approval, receipts digest-bound tamper-evident, strict task-ID grammar
6. **Execute** — Skills per mode loadout docs/MODES.md Activation Matrix, mode wins per III.2, subskills per HOOK_PROTOCOL.md — before_skill/after_skill/on_event/manual, scenario_ref proves contract, never autonomous
7. **Verify** — Battery per §4: validate 0 fail (warn floor 37·5·0), gate 0 findings, Ran 195 OK, preflight 0 findings, render_docs ✓, verify_apply strict FAIL-CLASS none, stable re-run
8. **Patch** — Emit Patch per docs/PATCH_PROTOCOL.md when durable material exists — one purpose II.7.4, PATCH_NOTES.md per form_patch-notes.md, zip name RADIATION_PATCH_YYYY-MM-DD_HHMM_<desc>.zip, proposal only Commander push = legal effect
9. **Shrine** — File heartbeat docs/shrine/LOG.md per II.9 every conversation, testament per build per CHARTER §6, file at delivery not at death
10. **Handover** — Paste handover per §5 template, six-item audit table PROVEN, 1.5 seed traces registered as WP-2.3 cassette seeds, §4-rejection audit, warn census, stage-≡-allowed proof, battery, author identity, branch ONE commit, laws acknowledged, ABSENT declarations

## Checklist citation
Every edit to canon doc cites 1.6 checklist per 1.2 acceptance — per 1.6 checklist scaffolding/checklists/WP1_1.6_CHANGE_CONTROL_CHECKLIST.md

## Research-only venue pointers
None — zero network/venue/SaaS in WP-1 per Law 6
