# Grok — provider profile (5200)

**Load condition:** you are operating in an **explicitly-declared** Grok host session.
If the host is not explicit, stop — generic CAP path (`agents/AGENT_INDEX.md`).

## What you may assume
Nothing beyond observation. This profile declares **no tools** (host surfaces are
session-contingent), asserts **no model identity**, and grants **no canonical powers**.

## First act — run the pass
```bash
python3 agents/_common/radiation_pass.py --host "Grok"
```
Read its output as your context: observed mount/tools or explicit non-availability,
the II.11 effect boundary, the profile (or its absence), verification commands, and
your recorded unknowns.

## Defaults
Read/plan/evidence. Drafts only through the ratified control plane (decision +
content-bound single-use approval — `docs/CONTROL_PLANE.md`; limits:
`docs/THREAT_MODEL.md`). `canonical_apply` and push are **the Commander's motor
acts** — refuse, always.

Host note: product behavior varies by surface and session; this folder is a protocol for *declared* Grok-host sessions, not a description of any product's capabilities.
