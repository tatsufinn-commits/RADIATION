# CONTRACTS — Per-Area Detail (WP-1 1.2)
**Basis:** AGENTS.md layered cascade per 1.6 checklist scaffolding/checklists/WP1_1.6_CHANGE_CONTROL_CHECKLIST.md
**Law:** D1–D10 · six ratified laws D029 · FF-seal only · raise-only ratchet

## Machine-checked agreements

### Two-key resolver (II.11)
- Policy (source rank vs executed allowlist) AND tool — --source label is assertion not credential
- No execution without chained decision + unconsumed content-bound single-use approval
- Drafts root only, no subprocess/network/canonical writes per executor bounds
- canonical_apply/push remain Commander motor acts per II.11

### Receipts (II.11)
- Tamper-evident hash-linked, NOT immutable per THREAT_MODEL.md
- Limits: docs/THREAT_MODEL.md, CI check 37 control plane
- Genesis = ratification, chain preserved

### Task-ID grammar (II.11)
- Schema-enforced, genesis exception, strict task grammar + pinned drafts base
- Content-bound single-use approvals

### Provider contracts (Dim-1/G4)
- agents/contracts/AGT-<slug>.json typed contracts bounded by receipts
- D4 law no_simulated_commander_authority true, machine-checked per check 38
- Summarize profiles, never upgrade tiers

### Tool registry (G4)
- tools/TOOL_REGISTRY.json bounded contract schema-EXECUTED + code-level rules self-tested per check 39
- 24 schemas within executor's executed set + legal annotations per check 40
- Discoverable tests exist and pass per check 39

### Release Truth Gate (5824 FIX)
- docs/RELEASE_TRUTH_GATE/EXPECTATION.json — base_sha + allowed_changes + required deletions + generated artifacts + mandatory validations + risks
- Gate live check + self-test 11 vectors, preflight 7 vectors per check
- Forbidden paths, generator/must_match_live enforced
- Moving-base law (new for build era): base rides last green seal from here on, union-carry era over per §4

### Patch self-certification (1.1)
- Six checkboxes ✅/❌ example each, teeth RETURNED not queued, Commander approval only sign-off per 1.1
- Template + three exemplar filled manifests + return-rule recorded per 1.1 acceptance

### Verification policies (1.5)
- Additive optional verify field on every subskill/cue, namespaced extension tolerating unknown keys, existing linters must not redden per 1.5
- Router honors required/opt-in/opt-out, three minimal golden traces one per policy class stdlib-asserted, registered as WP-2.3 cassette seeds per J1

## Research-only venue pointers
None — zero network/venue/SaaS in WP-1 per Law 6
