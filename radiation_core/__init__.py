"""radiation_core — shared machine layer (patch 4400, auditor plan §2/§6).
Stdlib only. relay.py = semantic task-bundle validation (check 27's engine).
The Markdown neurons remain the human-readable projections; the bundle in
evidence/tasks/<TID>/ is the authoritative lifecycle record."""
