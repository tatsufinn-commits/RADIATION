5300 Sanction — build-note draft (canonical bundle #10).

Declaration, not deletion: the course corpus and the twelve archive replica pairs
are admitted by hash-bound manifest; every undeclared shape still fails.
This draft was written through the control plane so the receipt chain itself
dogfoods the new semantic binding (execution binds decision + approval BY DIGEST,
approval single-use, bounds recorded, written entries draft-confined).
