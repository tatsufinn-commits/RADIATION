5500 Gatewright — build-note draft (canonical bundle #12).

The gate review's findings became regressions: transactional preflight,
executed schemas (oneOf/maxItems/bool), digest-consistent semantic tampering
caught, per-provider profile truth, zero-test discovery a crime. Candidate C
catalog normalization waits for its own Commander order (review gate 8).
