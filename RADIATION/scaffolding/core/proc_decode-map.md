# 🔬 DECODE MAP (`proc_decode-map.md`)
**Mode:** @Decode | **Mandatory for:** every repository decode

```text
□ 1. TARGET DECLARATION — repo(s): <url> | scope: full tree / declared subset
     (scout-vetted): <…>
□ 2. TREE SWEEP        — full structure read; inventory: <n> files, <n> dirs;
     entry points: <paths> | build/config files: <paths>
□ 3. ARCHITECTURE MAP  — layers/modules and their relationships (diagram or
     structured list); evidence: file paths per component
□ 4. INFRASTRUCTURE INVENTORY — stack, dependencies, tooling, CI, tests:
     each item + path evidence
□ 5. CONCEPT EXTRACTION — the ideas worth understanding/reusing:
     K<n>: <concept> | where: <paths> | why it matters: <…>
□ 6. PATTERN / ANTI-PATTERN REGISTER — P<n>/AP<n>: <pattern> | evidence: <paths>
□ 7. DEPENDENCY GRAPH  — internal module deps + external deps (with versions)
□ 8. SECONDARY CORROBORATION — min 8 secondary sources (docs/issues/papers/
     comparable repos) logged per proc_research-sortie entry format
```
FADE: outputs assemble into the msr.md-styled deliverable; sheet → short_term.
