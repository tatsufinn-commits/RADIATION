# Brain/short_term/ingest/ — curator's landing zone
Structured ingestion files: `YYYY-MM-DD_<source-slug>.md`, each with the
mandatory provenance header (origin | type | tier | date | mode) per
`proc_ingestion-run.md`. Secondary sources grade-capped [O]/[N] until
triangulated (I.2). Curator fills this funnel; triangulation purifies it (II.6).
