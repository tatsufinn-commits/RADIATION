# 🚫 ANTI-PATTERNS REGISTER (`docs/ANTI_PATTERNS.md`)
## RADIATION's Own Documented Failures — Named, Evidenced, Machine-Checked
**⚠️ APPEND-ONLY (II.2). REFERENCE file — never added to the boot set (P-09 crit. 6).**
**Rule for this register:** cures must be MACHINE-CHECKABLE (a validator check ID
or a countable git/ledger fact). An entry whose cure is "be careful" is rejected —
that is cargo-culting in a different costume. These seven are RADIATION's own
failures, deliberately NOT copied from TAMAKEE's A1–A16 (different subject).

| ID | Pattern | The instance (path + date) | The check that prevents it |
|---|---|---|---|
| AP-01 | **Legislative accretion** — canon grows because writing law is free, permanent, and feels like progress | 18 patch rows filed 2026-09-12 alone (`docs/PATCH_LEDGER.md`); II.8 was the THIRD attempt at the same transport-artifact failure class | check 16 (meta-budget ratio) + EVAL-FIRST clause (staged, P-09) |
| AP-02 | **Register theater** — doctrine cites registers that are empty | `cue/autopilot-doctrine.md` §2 cited `07-inspect/DEBT_REGISTER.md` / `docs/DECAY_REGISTER.md` rows that did not exist until the P-01 hygiene pass, 2026-09-12 | check 10 (register liveness) |
| AP-03 | **Grade inflation through convenience** — weak sources wearing strong grades | S003 findings F-01…F-07: `[R]` granted to grokipedia/Flickr/Fiveable/Scribd (`Brain/long_term/audits/PLANNING_reviewer_AUDIT_2026-09-12.md`, 2026-09-12) | PENDING — P-07 publisher rule + validator blocklist (honest gap: no check exists yet) |
| AP-04 | **Transport artifacts** — files that exist only to move content into git | root `PATCH_NOTES.md` + `append_blocks/` recurred twice pre-II.8 (`docs/CHANGELOG.md` v1.5.x entries, 2026-09-12) | check 3 (transport-artifact scan) |
| AP-05 | **Mirror duplication** — the same object living at two paths | TAMAKEE's 14 byte-identical file pairs (scavenged cautionary evidence, campaign index 99_SCAVENGE_MAP) — pre-emptively guarded here | check 11 (hash-duplicate detection) |
| AP-06 | **Placeholder ledgers** — records naming things that do not exist | 15 task-ledger rows carried invented `_1500_`-style patch names until backfilled 2026-09-12 (`Brain/frontal_lobe/task_ledger.md`) | check 6 (placeholder identifiers) |
| AP-07 | **Governance over output** — the system's own maintenance becomes the content | 4 of 4 sessions spent on the system; 0 Core cards in `09-nota/`; 9 core scaffolds unused (state at campaign start, 2026-09-12) | check 15 (boot-byte caps) + check 16 (meta-budget) + campaign milestone gate |

**2026-09-12 UPDATE (append, II.2):** AP-03's cure is now LIVE — validator check 19 (P-07 publisher blocklist); seeded-violation fixture demonstrated FAIL on 2026-09-12. The "PENDING P-07" note in the table above is superseded by this row.
