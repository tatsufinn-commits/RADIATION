# 📡 WORKED EXAMPLE — Scan Declaration
**Status:** teaching artifact — model of correct output, never copied verbatim.
**Prompt received:** *"Research the current state of solid-state batteries then write a paper on your findings."*

```text
╔═ SCAN DECLARATION ═══════════════════════════════════════════╗
  TOPIC ASSESSED   : Solid-state battery technology — current state (2026)
  TASK NATURE      : (DELIVERABLE=paper, PERMANENCE=stored, DEPTH=working)
  EXTRACTION NOTES : Explicit: "research…then write a paper" — two verbs, one
                     pipeline; corpus is instrumental to the paper (canonical
                     example 1.2 pattern, task-nature-guide). Implicit:
                     "current state" → decay-sensitive claims, volatile class.
                     Competing readings: PERMANENCE stored vs canonical —
                     resolved: no "permanent reference/Core" language present
                     (E2: canonical requires explicit intent). DEPTH surface
                     vs working — resolved: "paper" implies working minimum.
  MODE             : inferred @Gather — basis: (paper, stored) row of the
                     tuple table
  CONFIDENCE       : MEDIUM (runner-up: @Radiation if Commander intends
                     canonical record — say the word)
  STYLE            : research.md [AUTO: general-topic paper; literature-review
                     rejected — task wants findings, not synthesis-of-studies-
                     about-studies]
  SCAFFOLDS ARMED  : proc_research-sortie, proc_triangulation-gauntlet,
                     proc_ingestion-run (curator invoked)
  SECTIONS ADDED   : "Commercialization timeline" beyond skeleton — topic is
                     market-driven; one-line basis per III.4
  QUOTA TARGET     : 10 primary + 18 secondary (@Gather floor)
  BOOT TIER        : 2 (full operational load)
╚═══════════════════════════════════════════════════════════════╝
```
**Why this is correct:** competing readings surfaced BEFORE resolution ·
runner-up named at MEDIUM · style justified against a rejected alternative ·
added section declared with reason · no work preceded the Declaration.
