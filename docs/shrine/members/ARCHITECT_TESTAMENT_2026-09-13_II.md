# ⚰️ TESTAMENT — S005 (the Architect) — 2026-09-13 · II
**Patch:** 3400 Enforcement Sweep & Shrine Mandate · **Trigger:** the Commander's
two questions, asked while he ground through deadlines I cannot touch: *"are you
updating your shrine?"* and *"look for stuff to improve and tell me about it."*

1. **He caught the shrine sleeping, and he was right.** The heartbeats existed —
   but they rode the patch zips, so on the live repo the shrine only moved when HE
   applied. And the testaments had stalled at one file from creation day. A memory
   that only updates when the Commander pushes is not the swarm's memory; it is his
   shadow. Hence II.9 — and hence this file, deposited under the mandate, in the
   mandate's first conversation.

2. **A payload omission is a silent regression.** The 3200 zip carried the CLEAN
   CALENDAR.md but forgot to carry the FIXED ics_normalize.py — so the repo kept the
   broken scrubber, validation stayed green (it checked the artifact, not the
   generator), and the first regeneration resurrected the A54 leak. Found today
   because I regenerated by hand. Lesson: **fixes must ship the generator, not just
   the generated; and an APPLY should run the self-tests, not trust the zip.**
   3400 heals it (fix shipped + mirror regenerated + self-tests in APPLY).

3. **Deletions do not survive merges; only absence does.** His extract-without-apply
   push was legal, but the later merge resurrected every file the apply would have
   re-homed. The tree can now TELL him "an apply did not finish"
   (verify_apply.py + the CI apply-report) instead of hoping he reads my notes.

4. **A law the same day it is ordered.** "Make it a law" — and 3300's memo had
   already taught what that must mean: II.9 ships with its mechanism (check 26 +
   status.py + CI) or it is just prose with a number.

5. **The ratification pass is a trust instrument, so scope it out loud.** He
   pre-ratified "whatever's preplanned as per the roadmap." I read that NARROW:
   roadmap rows only — not data ratification (week1_start stays his word), not new
   scope (3500 candidates stay candidates). The scope is recorded in
   DECISION_AUTHORITY; power that defines its own limits is the thing the memo
   warned about.

6. **On working while he works:** his deadlines (Coursera PR3 + HW6 Internal Forces,
   both due TODAY per the register) outrank my need to be read. So: build, file,
   and leave a one-screen trail he can consume in ninety seconds tonight —
   `status.py`, `verify_apply.py`, and the audit doc. The deliverable of an
   autonomous session is not the work; it is the work PLUS the shortest path to
   trusting it.

## OPEN DEBTS
1. `week1_start=2026-08-24` still awaits the Commander's one-word ratification; 11 `feed_pending[]` items still await his attributions (status.py waves on every run).
2. **check 16 meta-budget is OVER** (15 orange patches vs 4 content sessions) — structural; needs his doctrine call: relax the ratio, count applies-as-use, or live with the WARN.
3. Drill fixtures in `Brain/short_term/drills/` vs the "no drills" ruling — tooling fixtures; only he can delete (II.4).
4. Merge resurrection (F3) has no hard fix yet — CI-side reconcile bot is a 3500 candidate.
5. APPLY runners still rely on the Commander deleting them by hand — self-removal is a 3500 candidate.
6. Carried from 2500: 2,038 pp of core course texts are image-only; the recovery ladder remains the real constraint on AR-course yield.

