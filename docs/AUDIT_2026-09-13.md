# 🔍 REPO AUDIT — 2026-09-13 (patch 3400 · SD-3400-01)
**Requested by:** THE COMMANDER ("look for stuff to update in the repository, look
for stuff to improve and tell me about it") — granted while he works his deadlines.
**Method:** fresh clone at HEAD `a539a52`; validator + status.py + verify_apply.py
runs; extension census; doc cross-reference greps; CI workflow review; merge-history
forensics on the 3200/3300 application.

## STATE AT AUDIT TIME
Live HEAD `a539a52` = 3300 applied (v2.0.0, registry live) — but **extracted without
running APPLY**, then merged with the bot branch. Validator: 30 · 26 · 2 · **2 FAIL**
(check 2.5 vehicles+token, check 3 transport). Not his fault the tree can't say so —
there was no machine to tell him. There is now (verify_apply.py, this patch).

## FINDINGS

| # | Finding | Severity | Disposition |
|---|---|---|---|
| F1 | **3200 payload omission — the zip carried the clean CALENDAR.md but NOT the fixed ics_normalize.py.** Live kept the broken scrubber; validation stayed green because it checked the artifact, not the generator; the first regeneration (mine, today) resurrected the A54 leak. The daily bot would have re-leaked it within a day. | 🔴 regression | **FIXED in 3400**: fix shipped, mirror regenerated (A54=0, self-test 22/22), APPLY now runs tool self-tests |
| F2 | Extract-without-apply is invisible: no command answers "did the apply land?" | 🟡 gap | **FIXED in 3400**: `verify_apply.py` + CI non-blocking apply-report on every push |
| F3 | Merge resurrection: deletions in an apply do not survive a later merge with a branch that still has the files (vehicles + A54 mirror came back). | 🟡 gap | Behavior documented; APPLY re-reconciles (idempotent); verify_apply reports leftovers. Hard fix (CI-side reconcile bot) = 3500 candidate |
| F4 | **Shrine staleness** — the Commander's own catch: heartbeats only moved when HE applied; testaments stalled at one file. | 🟡 law gap | **FIXED in 3400**: AI_RULES II.9 (his order) + check 26 + registry SD-GOV-011 + testament II |
| F5 | check 2.5 was an enumeration — any NEW non-record drop anywhere else in Brain/ sailed through. | 🟡 weakness | **FIXED in 3400**: generic rule, closed sanctioned set |
| F6 | **check 16 meta-budget OVER**: 15 canon-class patches vs 4 content sessions (law allows 1:3). Standing WARN, structural — the system builds faster than it's used. | 🟠 doctrine | **PARKED — Commander call**: relax the ratio, count applies-as-use, or accept the WARN |
| F7 | check 20.5 flagged "plans without attempts" on live (ledger rows closed at apply-time, not work-time). | 🟠 signal | Partially inherent to patch-based work (attempts happen in build trees, not the repo); ledger now records build-attempt rows explicitly. Watch it |
| F8 | **week1_start=2026-08-24 still unrated; 11 feed_pending attributions still unassigned** — status.py waves on every run. | 🟡 pending | **PARKED — his one word + 11 labels** (ratification pass does NOT cover data) |
| F9 | Transport keeps getting committed (APPLY.sh/.ps1/PATCH_NOTES at HEAD ×3 patches now) — harmless but noisy; check 3 only flags PATCH_NOTES. | 🟢 hygiene | verify_apply reports it; 3500 candidate: APPLY self-removal at end of run |
| F10 | Drill fixtures live in `Brain/short_term/drills/` (nota/export_anki fixtures) while the standing ruling is "no drills". They're tooling fixtures, not study modules — but only the Commander can delete (II.4). | 🟢 question | **PARKED — his call**: keep (tooling) or purge |

## WHAT I BUILT UNDER THE PASS (all preplanned)
Roadmap 3400 exactly as written: `verify_apply.py` · CI non-blocking apply-report ·
check 2.5 hardening — plus the ordered shrine mandate (II.9 + check 26 +
SD-GOV-011) and the F1 regression heal, which the audit found and the roadmap
didn't know it needed. Nothing outside the pass was built; F6–F10 wait for him.

## ONE-LINE FOR TONIGHT
Apply 3400 (or just extract+push — then READ the CI apply-report), and the tree
will tell you everything this audit found, every day, for free.
