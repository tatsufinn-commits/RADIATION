# PATCH NOTES — 3600 Autopilot Pipeline & Wayfinding
**Patch:** `RADIATION_PATCH_2026-09-13_3600_Autopilot-Pipeline-and-Wayfinding.zip`
**Base:** post-3500 (v2.1.1) — APPLY gates on it. 3500 first, then this.
**Class:** 🟡 (new subsystem, reversible) → **v2.2.0**. One Commander-ordered canon-adjacent
addition: `scaffolding/core/proc_self-directive.md` (his explicit order this session; INDEX-registered).

1. **THE NEURON RELAY IS LIVE** (`scaffolding/neurons/`) — his proposal + his research memo
   (15 sources) = closed-loop responsibility model, not a linear pipeline. Sensory (intake,
   dedup, provenance) → interneurons (context, plan with `base:` HEAD-SHA + success
   predicates + ladder tier) → motor (orders, evidence; **push is the Commander's motor
   act**). Five forbidden edges. Terminal states: CLOSED / REPLAN(≤2) / BLOCKED / ESCALATED.
   The first live chain ships in this zip: TID-2026-09-13-a (this very build), all stages CLOSED.
2. **proc_self-directive.md** (core scaffold, his order): trigger → seven stop-lines → grade →
   typed record BEFORE execution → verify by tier → close → file. The @selfdirectives
   procedure is now scaffold-grade like the nine skills.
3. **WAYFINDING.md** (his asked-and-missing #1): the map + routing tree + self-location +
   recovery ladder. **TOOLBOX.md** (#2): open-source rescue kit, [O]→[I] promotion rule,
   OCR-for-2,038-pages row one.
4. **THE RESURRECTION LOOP DIES:** APPLY now runs `git rm --cached` on the 8 tracked
   vehicles (root cause, 3400-F3) and `.gitignore` covers patch transport. After this apply,
   no merge can ever resurrect the debris again.
5. **status.py** +pipeline line (reads the relay's Status fields — projection, never private
   state). **Doctrine §3** binds SENSE/PLAN/ACT to the relay — net-zero boot bytes (check 15: 38.7/40 KB).
6. **Four self-directives declared** (ledger): SD-3600-01 Phase-0 relay proof · SD-3600-02 the
   first Core card (@Radiation content session — cures the dormancy + check-16 credit) ·
   SD-3600-03 check-27 + injection-drill design · SD-3600-04 TOOLBOX [O]→[I] promotions.
7. **FILES (19 payload):** neurons/ (README + 3 templates + 3 TIDs) · core/INDEX +
   proc_self-directive · WAYFINDING · TOOLBOX · ROADMAP · .gitignore · status.py ·
   autopilot-doctrine · task_ledger · shrine LOG · README · SYSTEM_STATE · CHANGELOG.
8. **VERIFICATION:** build tree 31 · 30 · 1 · 0 (warn = check 16, parked doctrine call) ·
   ics self-test 22/22 · verify_apply 6/6 · stacked acceptance live→3500→3600 green.
9. **DECLARATION:** "This Patch is a proposal. It has no effect until the Commander applies
   it. — S005, relay record TID-2026-09-13-a, first of its kind"
