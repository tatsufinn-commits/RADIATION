{
 "$schema": "https://json-schema.org/draft/2020-12/schema",
 "title": "radiation.model_research_register/0.1",
 "description": "Typed source register for the Candidate C catalog (5710): every evidence source_id in every record must resolve here, with matching tier and a real calendar date. The checker executes this schema and binds records to it. Rows are dated facts about SOURCES, not conclusions.",
 "type": "object",
 "required": ["schema_name", "generated_by", "sources"],
 "additionalProperties": false,
 "properties": {
  "schema_name": {"const": "radiation.model_research_register/0.1"},
  "generated_by": {"type": "string", "minLength": 8, "maxLength": 160},
  "sources": {
   "type": "array",
   "minItems": 1,
   "maxItems": 200,
   "items": {
    "type": "object",
    "required": ["id", "tier", "url", "retrieved_on", "title"],
    "additionalProperties": false,
    "properties": {
     "id": {"type": "string", "pattern": "^[OSU][0-9]{1,3}$"},
     "tier": {"enum": ["O", "S", "U"]},
     "url": {"type": "string", "minLength": 4, "maxLength": 300},
     "retrieved_on": {"type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
       "description": "ISO calendar date; the checker validates the day exists"},
     "title": {"type": "string", "minLength": 4, "maxLength": 200},
     "note": {"type": "string", "minLength": 2, "maxLength": 300}
    }
   }
  }
 }
}
