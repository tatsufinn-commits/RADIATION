{
 "$schema": "https://json-schema.org/draft/2020-12/schema",
 "title": "radiation.tool_io.validate.inputs/1",
 "description": "Invocation record for validate. CLI argv only; cwd is the repo root. No stdin envelope. Live flags at 0438820.",
 "type": "object",
 "additionalProperties": false,
 "required": [
  "schema_name",
  "cwd",
  "argv"
 ],
 "properties": {
  "schema_name": {
   "const": "radiation.tool_io.validate.inputs/1"
  },
  "cwd": {
   "const": "repo-root"
  },
  "argv": {
   "type": "array",
   "maxItems": 4,
   "items": {
    "enum": [
     "--json",
     "--profile",
     "--self-test",
     "--tree"
    ]
   }
  }
 }
}
