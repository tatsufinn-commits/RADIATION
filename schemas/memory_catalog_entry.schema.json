{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "radiation.memory_catalog_entry/1 — Brain Retrieval Lattice entry",
  "description": "Typed record over existing Brain/ corpus (and shrine log) for deterministic retrieval. P-14 G5: corpus behavior from absence to mechanism. Each entry points to existing path, has id MEM-<kind>-<slug>, title, kind enum, tags ≥3, added date. Sorted by id, unique.",
  "type": "object",
  "required": ["id", "path", "title", "kind", "tags", "added"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^MEM-(course_derivative|ledger|plan|shrine|note)-[a-z0-9-]+$",
      "description": "id format MEM-<kind>-<slug-or-NNN>, unique, sorted by id"
    },
    "path": {
      "type": "string",
      "pattern": "^(Brain/|docs/shrine/).+",
      "minLength": 1,
      "description": "existing Brain/ path (or docs/shrine/LOG.md for shrine log)"
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "description": "human-readable title"
    },
    "kind": {
      "type": "string",
      "enum": ["course_derivative", "ledger", "plan", "shrine", "note"],
      "description": "kind of corpus entry"
    },
    "tags": {
      "type": "array",
      "minItems": 3,
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9-]+$",
        "minLength": 1
      },
      "description": "at least 3 tags, lowercase alphanumeric/hyphen"
    },
    "added": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "date added YYYY-MM-DD"
    }
  }
}
