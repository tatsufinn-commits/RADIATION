# Brain/short_term/active/ — matter under active evaluation
Claims and material currently being worked: `[UNVERIFIED — attempt n]` claims
between triangulation attempts (auto-demote to subsidiary after 2 failures),
and information in active use this session.

S-2-ENV (v3.10.26): session_capability_state.json (schema `radiation.session_capability_state/1`) is working memory written via `agents/_common/radiation_pass.py --persist` — session-local, presumed impermanent, triaged at session close per short-term law, never committed with live content; UNKNOWN first-class, stale reads UNKNOWN, OBSERVED must cite cap_probe, host_label DECLARED, file can never raise authority, no hardcoded limits, input artifact for scan/anchor/stockpile shortfall never resolver.
