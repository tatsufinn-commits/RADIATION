# schemas/ — pointer (declared deviation from the example layout)

The record schema lives at the repo-central `schemas/model_research_record.schema.json`
so that check 40's keyword-coverage scan and the ONE schema executor see it —
schemas outside that directory would escape the coverage law. This catalog's
`schemas/` directory holds this pointer only.
